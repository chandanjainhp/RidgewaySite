# Incident detail — case file

Two-pane layout: left is the narrative (title, severity, evidence chain,
agent reasoning, uncertainties, three action buttons). Right is the
aside (mini-map, raw events, entities).

Links back to `../dashboard/index.html` via the back chevron.
