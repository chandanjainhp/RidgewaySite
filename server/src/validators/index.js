const eventValidator = require('./event.validator');
const incidentValidator = require('./incident.validator');
const investigationValidator = require('./investigation.validator');
const briefingValidator = require('./briefing.validator');
const reviewValidator = require('./review.validator');

module.exports = {
  eventValidator,
  incidentValidator,
  investigationValidator,
  briefingValidator,
  reviewValidator,
};
